spec_id: "intent-02-05-integrator-001"
type: intent
state: active
intent:
  summary: "Merges approved PRs in topological dependency order with dry-run conflict detection"
  detail: |
    Merges approved PRs in topological dependency order. Dry-run conflict
    detection via git merge --no-commit --no-ff. Conflict triggers task
    re-queue with fresh spawn (not failure). 20%+ tasks with conflict
    retries → retrospect phase flags a process issue.
    Project-specific merge heuristics come from project.yaml conventions.
constraints: []
indexing:
  type: intent
  priority: high
  layer: feature
  domain: "integration"
  tags: [integrator, merge, conflict-resolution, topological-sort]
provenance:
  source_type: codebase_extraction
  confidence: 0.95
  source_ref: "harness-system codebase 2026-04-19 audit"
relations: []
anchors:
  - file: "agents/integrator.md"
  - file: "apps/server/src/services/launcher/spawner.ts"
